if "testfile.txt.a" in listfiles():
  removefile("testfile.txt.a")
if "testfile.txt.b" in listfiles():
  removefile("testfile.txt.b")

f = ABopenfile("testfile.txt", True)

# Initial A file should be valid
assert(f.readat(None, 0) == "SE")

# B is empty, so offset 0 is legal
f.writeat("S", 0)

# B length is now 1.
# Offset 1 is NOT past EOF; it is exactly the append position.
f.writeat("123", 1)

# B length is now 4.
# Offset 4 is exactly the append position.
f.writeat("E", 4)

f.close()

f = ABopenfile("testfile.txt", False)

assert(f.readat(None, 0) == "S123E")

f.close()
